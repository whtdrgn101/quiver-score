# Overlay to run Go API as primary with Python API as backend.
# Usage: docker compose -f docker-compose.yml -f docker-compose.go.yml up -d
#
# Go API listens on :8080 (public), proxies non-Go routes to Python API on :8000.
# Python API is internal only (no published ports when using this overlay).
services:
  api:
    ports: !override
      - "8001:8000"

  api-go:
    build: ./backend-go
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: postgresql+asyncpg://quiverscore:quiverscore@db:5432/quiverscore
      SECRET_KEY: dev-secret-key-change-in-production
      CORS_ORIGINS: "http://localhost:5173"
      PORT: "8080"
      PYTHON_API_URL: "http://api:8000"
      RATE_LIMIT_ENABLED: ${RATE_LIMIT_ENABLED:-true}
    depends_on:
      db:
        condition: service_healthy
      api:
        condition: service_started
    restart: unless-stopped
